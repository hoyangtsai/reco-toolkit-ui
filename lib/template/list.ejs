<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=no"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title><%= htmlWebpackPlugin.options.title %> 列表</title>
    <style>
      body {
        font-family: Roboto, "Droid Sans", "Droid Sans Fallback", "Helvetica Neue", Helvetica, STHeiTi, sans-serif;
      }
      h1 {
        text-align: center;
      }
      h3 {
        margin: 0.8em 0 0.6em;
      }
      li {
        text-decoration: none;
        color: #939393;
      }
      ul {
        padding-left: 1.8em;
      }
      li {
        margin: 0.6em 0;
      }
      li:hover > a:link,
      li:hover > a:visited,
      li:hover > a:hover,
      li:hover > a:active,
      li:active > a:link,
      li:active > a:visited,
      li:active > a:hover,
      li:active > a:active {
        color: #00adff;
      }
      a:link,
      a:visited,
      a:active {
        color: #4c9afa;
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <h1><%= htmlWebpackPlugin.options.title %></h1>
    <ul>
      <% var groups = htmlWebpackPlugin.options.groups; %>
      <% for (var i in groups) { %>
        <% if (typeof groups[i] && typeof groups[i] === 'string') { %>
          <li><a href="./<%= groups[i] %>"><%= groups[i] %></a></li>
        <% } else if (typeof groups[i] === 'object') { %>
          <% if (groups[i].title) { %>
            <li>
              <h3><%= groups[i].title %></h3>
              <ul>
                <% for (var j in groups[i].pages) { %>
                  <li><a href="./<%= groups[i].pages[j].path %>.html"><%= groups[i].pages[j].name %></a></li>
                <% } %>
              </ul>
            </li>
          <% } else { %>
            <% for (var j in groups[i].pages) { %>
              <li><a href="./<%= groups[i].pages[j].path %>.html"><%= groups[i].pages[j].name %></a></li>
            <% } %>
          <% } %>
        <% } %>
      <% } %>
    </ul>
  </body>
</html>
